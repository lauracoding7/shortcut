<%= simple_form_for [@user, @review] do |f| %>
  <%= f.input :content %>
  <%= f.input :rating, collection: 1..5 %>
  <%= f.submit "Create review", class: "btn btn-danger" %>
<% end %>
<%= link_to 'Go back', dashboard_path %>
