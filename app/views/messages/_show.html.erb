<ul><strong>
  <li class="card-message"><%= message.content %></strong>


  <em>written by <%= message.author.name ? message.author.name : message.author.email %>, <%= Time.now.strftime("%B %e at %I:%M %p")%></em></li>
</ul>

