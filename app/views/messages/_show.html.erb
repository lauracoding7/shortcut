<div class="row">
  <div class="col-xs-offset-2 col-xs-8">
    <div class="sent-message">
      <p><%= message.content %></p>
      <p class="message-details"><em>written by <%= message.author.name ? message.author.name : message.author.email %>, <%= Time.now.strftime("%B %e at %I:%M %p")%></em></p>
      <!-- here it is not the right time -->
    </div>
  </div>
</div>



